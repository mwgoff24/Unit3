'''
#################
Do Now 3.01
#################
1.  In your Console
Type the following code:
    1   import random
    2   random.randint(0, 3)
    3   random.randint(0, 3)
    3   print(random.randint(0, 3))
    4   print(random.randint(0, 3))
    5   print(random.randint(0, 3))
2.  In your Notebook
Respond to the following
    1.  What does randint() do? 
    Generates random integers from the first value in the argument to the second value in the argument.

    2.  What do the values 0 and 3 do? Try changing those numbers, rerun the program, and write down what changed.
    Sets the range of possible random values to return

    3.  What is the difference between random.randint(0,3) and print(random.randint(0,3))?
    First one only returns a value but will not display in console, second one will show value in console.
'''